# Adding notes

1. Pick the right section folder.
2. Create a `topic-slug/` folder.
3. Copy the two files from `_templates/`.
4. Fill them in and update the links in the section `README.md` and the master `README.md`.
5. Commit with a message like `notes: add <topic>`.
